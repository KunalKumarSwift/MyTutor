# SQLite (default — no server needed)
DB_DRIVER=sqlite
# DB_FILE=/absolute/path/to/database/mytutor.db   # optional, defaults to database/mytutor.db

APP_URL=http://localhost:8080/

# --- MySQL (uncomment to use MySQL / Docker instead) ---
# DB_DRIVER=mysql
# DB_HOST=127.0.0.1
# DB_NAME=MyTutor
# DB_USER=mytutor
# DB_PASSWORD=secret
